# Optional. Better Auth signing secret for production.
# Generate with: openssl rand -hex 32
AUTH_SECRET=

# Optional server-wide Gemini key. Students can also paste their own in Settings.
GEMINI_API_KEY=
